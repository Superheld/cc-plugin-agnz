{
  "description": "agnz mailbox delivery + lead-context discipline: injects unread messages addressed to `parent` from <cwd>/.claude/agnz/messages.jsonl into Claude's context on prompt submit and session start, and fences the lead's Read tool (and large-context Grep) off agnz thread transcripts/traces (ADR 0015). Fast no-op when the current project has no agnz workspace.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Read|Grep",
        "hooks": [
          {
            "type": "command",
            "command": "node ${CLAUDE_PLUGIN_ROOT}/scripts/hooks/pre-tool-use.mjs",
            "timeout": 5
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "node ${CLAUDE_PLUGIN_ROOT}/scripts/hooks/user-prompt-submit.mjs",
            "timeout": 5
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "node ${CLAUDE_PLUGIN_ROOT}/scripts/hooks/session-start.mjs",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
